import { View, Text } from 'react-native';
export default function AccountsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-gray-400">ربط الحسابات — قريباً</Text>
    </View>
  );
}
